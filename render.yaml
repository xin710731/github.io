services:
  - type: worker
    name: telegram-checkin-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_checkin_pro_v3.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_IDS
        sync: false
